<% flash.each do |key, value| %>
  <div class="alert alert-<%= key.to_s != 'notice' ? 'danger' : 'success'  %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <%= value %>
    </ul>
  </div>
<% end %>
