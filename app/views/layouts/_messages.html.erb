<% flash.each do |key, value| %>
  <div class="alert alert-<%= (key == 'notice') ? 'success' : key  %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <%= value %>
    </ul>
  </div>
<% end %>
